<body class="animals-index">
<div id="wrapper">
	<div id="columns">
    <% @animals.each do |animal| %>
		<div class="pin">
      <%= link_to image_tag(animal.image_path), animal_path(animal) %>
		</div>
    <% end %>
  </div>
</div>
</body>
