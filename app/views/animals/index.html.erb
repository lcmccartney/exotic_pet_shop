<h1 class= "text-center">Welcome to the Exotic Pet Shop</h1>
<br>
<div class="container">
  <div class="row">
    <% @animals.each do |animal| %>
    <%= link_to image_tag(animal.image_path, size: "200", class: "col-md-3 centered img-rounded"), animal_path(animal) %>
    <% end %>
  </div>
</div>
